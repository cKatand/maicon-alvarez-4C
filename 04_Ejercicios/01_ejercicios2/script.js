function temperatura(){
    let temp ="";
    let temperatura = prompt("Ingrese la temperatura actual");

    if (temperatura >= 25){
        temp = `Hace calor <h2>${temp}</h2>`;
    }else{
        (temperatura < 0)
        temp = `Hace frio <h2>${temp}</h2>`;
    }{
        (temperatura)
    }
    document.getElementById('lista').innerHTML = temp;
}ñ